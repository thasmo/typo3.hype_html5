styles {
	content {
		prefix = csc
		dateFormat = %x
		imagePath = uploads/pics/
		mediaPath = uploads/media/
		newWindow = 1
		useLightbox = 1
		tables = tt_content

		maxWidth = 600
		maxHeight = 1200
	}
}